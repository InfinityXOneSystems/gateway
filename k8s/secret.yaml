apiVersion: v1
kind: Secret
metadata:
  name: gateway-secrets
  namespace: infinity-gateway
type: Opaque
stringData:
  jwt-secret: "change-this-super-secret-key-in-production"
  # Add other secrets as needed
  # api-key: "your-api-key"
